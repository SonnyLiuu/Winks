<Window x:Class="WinksUI.AssistMenu"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:effects="clr-namespace:System.Windows.Media.Effects;assembly=PresentationCore"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WinksUI"
        xmlns:Controls="clr-namespace:RadialMenu.Controls;assembly=RadialMenu"
        mc:Ignorable="d"
        Title="AssistMenu" Height="300" Width="300"
        WindowStyle="None"
        AllowsTransparency="True"
        Topmost="True"
        Background="Transparent"
        Loaded="Window_Loaded"
        ShowInTaskbar="False">

    <Window.Resources>
        <Style TargetType="{x:Type Controls:RadialMenuItem}">
            <Setter Property="EdgeBackground" Value="Black" />
            <Setter Property="ContentTemplate">
                <Setter.Value>
                    <DataTemplate>
                        <TextBlock Text="{Binding}" Foreground="White" TextAlignment="Center"/>
                    </DataTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="Black"/>
            <Setter Property="EdgePadding" Value="0"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="EdgeBackground" Value="Gray" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Controls:RadialMenu x:Name="MyRadialMenu" IsOpen="{Binding IsOpen}">
            <Controls:RadialMenu.CentralItem>
                <Controls:RadialMenuCentralItem Command="{Binding CloseRadialMenuCommand}">
                    <TextBlock>Close</TextBlock>
                </Controls:RadialMenuCentralItem>
            </Controls:RadialMenu.CentralItem>
        </Controls:RadialMenu>
    </Grid>
</Window>